<Window x:Class="Generator.Client.Desktop.Views.ConfigurationEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Generator.Client.Desktop"
        xmlns:viewModels="clr-namespace:Generator.Client.Desktop.ViewModels"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:ConfigurationViewModel}"
		SizeToContent="WidthAndHeight"
        Title="Configuration editor" Height="450" Width="800">
	<Window.InputBindings>
		<KeyBinding Key="S" Modifiers="Control" Command="{Binding Path=CommitChangesCommand}" />
	</Window.InputBindings>
	<DockPanel>
		<Border Background="Gray" DockPanel.Dock="Bottom" Padding="5">
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
				<Button Content="Save" Command="{Binding Path=CommitChangesCommand}" Padding="5" Margin="0,0,5,0" />
				<Button Content="Discard" Command="{Binding Path=UndoChangesCommand}" Padding="5" />
			</StackPanel>
		</Border>
		<ScrollViewer  Padding="10">
			<StackPanel>
				<Label Style="{StaticResource LabelSmallStyle}" Content="Configuration name:" />
				<TextBox Text="{Binding Path=ConfigurationName, UpdateSourceTrigger=PropertyChanged}" />

				<Label Style="{StaticResource LabelSmallStyle}" Content="Solution path:" />
				<DockPanel>
					<Button DockPanel.Dock="Right" Margin="5,0,0,0" Content="..." Padding="5,0" Command="{Binding Path=SelectSolutionCommand}"/>
					<TextBox Text="{Binding Path=SolutionPath}" IsReadOnly="True" Background="LightGray" />
				</DockPanel>

				<Label Style="{StaticResource LabelSmallStyle}" Content="Icon package:" />
				<TextBox Text="{Binding Path=Icon.Package, UpdateSourceTrigger=PropertyChanged}" />

				<Label Style="{StaticResource LabelSmallStyle}" Content="Icon id:" />
				<TextBox Text="{Binding Path=Icon.Id, UpdateSourceTrigger=PropertyChanged}" />

				<Label Style="{StaticResource LabelSmallStyle}" Content="Name:" />
				<TextBox Text="{Binding Path=Name, UpdateSourceTrigger=PropertyChanged}" />

				<Label Style="{StaticResource LabelSmallStyle}" Content="DefaultName:" />
				<TextBox Text="{Binding Path=DefaultName, UpdateSourceTrigger=PropertyChanged}" />

				<Label Style="{StaticResource LabelSmallStyle}" Content="Description:" />
				<TextBox Text="{Binding Path=Description, UpdateSourceTrigger=PropertyChanged}" />

				<Label Style="{StaticResource LabelSmallStyle}" Content="ProjectType:" />
				<ComboBox SelectedValue="{Binding Path=ProjectType}" SelectedValuePath="Content" >
					<ComboBoxItem Content="CSharp"/>
					<ComboBoxItem Content="VisualBasic"/>
				</ComboBox>

				<Label Style="{StaticResource LabelSmallStyle}" Content="ProvideDefaultName:" />
				<CheckBox IsChecked="{Binding Path=ProvideDefaultName, UpdateSourceTrigger=PropertyChanged}" />

				<Label Style="{StaticResource LabelSmallStyle}" Content="CreateInPlace:" />
				<CheckBox IsChecked="{Binding Path=CreateInPlace, UpdateSourceTrigger=PropertyChanged}" />

				<Label Style="{StaticResource LabelSmallStyle}" Content="CreateNewFolder:" />
				<CheckBox IsChecked="{Binding Path=CreateNewFolder, UpdateSourceTrigger=PropertyChanged}" />
			</StackPanel>
		</ScrollViewer>
	</DockPanel>
</Window>
